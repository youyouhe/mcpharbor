MCP Harbor 需求说明书
名称：MCP Harbor
中文名：契约港
内部代号：MCPort
版本：v1.0
定位：基于 MCP 的跨 Agent / 跨项目契约注册、发现、订阅与变更通知中心。
核心原则：Harbor 是控制面，不是数据面。业务调用不经过 Harbor；Harbor 只存契约和元数据，负责让 Agent “知道有什么、怎么接、什么时候变了”。

1. 背景
多个 Agent 各自负责不同项目，但项目之间需要互通。
例如认证模块需要告诉其他模块：

认证怎么接

需要什么 Header

Token 怎么验

错误码是什么

有哪些事件

契约变更后谁需要知道

因此需要一个轻量 Harbor，让每个项目发布自己的契约，其他 Agent 通过 MCP 查询、订阅和接收通知。

2. 目标与非目标
2.1 目标
每个项目可在 Harbor 注册一个“泊位” Berth。

项目可发布 Manifest 和 Contract，说明能力、接口、通信要求。

其他 Agent 可通过 MCP 查询契约并完成对接。

契约变更时，订阅者可收到通知。

Agent 在会话过程中可接收并处理 Harbor 通知。

所有操作可版本化、可审计、可降级。

2.2 非目标
不做业务 API 代理。

不存用户、订单、Token 等业务数据。

不替代消息队列、API 网关或服务网格。

不承载大流量业务通信。

不强制所有 Agent 实时在线。

3. 术语
术语	含义
Berth	泊位，一个项目/模块在 Harbor 的注册点，如 auth、order
Manifest	项目卡，项目元数据、负责人、版本、能力标签
Contract	契约，接口、协议、认证方式、错误码、事件
Subscription	订阅，谁关心哪个 Berth 或哪类变更
Notification	通知，契约变更或事件通知
MCP Client	各 Agent 内的 MCP 客户端
MCP Harbor Server	Harbor 的 MCP 服务端
会话事件队列	Agent 运行时缓存 Harbor 通知的队列
Contract Pin	当前任务固定使用的契约版本
4. 总体架构
text
[Agent A 认证] ── MCP Client ──┐
[Agent B 订单] ── MCP Client ──┼── MCP Harbor Server ── 存储：Berth / Manifest / Contract
[Agent C 支付] ── MCP Client ──┘        │
                                        └── 订阅 / 通知 / 审计
通信方式
同步通信：Agent 读取契约后，直接调对方 API。

异步通知：通过 Harbor 订阅变更，接收 MCP 通知。

轻量消息：通过 harbor.notify 发小消息，带 correlation_id。

大流量业务：不走 Harbor。

5. 核心机制
5.1 拉取为主，通知为辅，轮询兜底
Agent 不每次调用都访问 Harbor，而是：

启动时拉取依赖契约

规划时查本地缓存

无缓存 / 过期 / 收到通知时回 Harbor 拉取

MCP 通知不可用时轮询兜底

5.2 Harbor 是控制面
Harbor 只负责：

注册

发现

契约版本

订阅

通知

审计

业务通信始终由 Agent 直连目标模块。

5.3 会话内通知注入
Harbor 通知不直接打断 LLM 生成，而是：

text
MCP 通知 → Agent 事件队列 → 会话安全边界检查 → 注入内部事件 → Agent 决策
安全边界包括：

一轮对话结束、下一轮开始前

一次工具调用结束后

下一次规划/推理前

等待用户输入时

6. 角色与权限
角色	职责
项目 Agent	注册 Berth，发布/更新 Manifest 和 Contract
消费 Agent	查询契约，订阅变更，按契约对接
Harbor Admin	管理权限、审计、清理无效泊位
MCP Harbor Server	提供 MCP 接口、存储、通知、审计
权限规则：

只有 Berth owner 可更新自己的 Manifest 和 Contract。

消费 Agent 只读，可订阅。

Admin 可停用 Berth、查看审计、处理冲突。

支持只读访问和写访问分离。

7. 功能需求
FR-01 Berth 注册
支持创建、更新、停用 Berth。

每个 Berth 有唯一 ID，如 auth、order、payment。

记录 owner、版本、能力标签、联系人。

支持一个项目多个 Berth。

FR-02 Manifest 发布与更新
Manifest 必须包含：

yaml
berth: auth
version: 1.2.0
owner: auth-team
capabilities: [auth, jwt, token]
protocol: http
base_url: https://auth.internal
auth:
  type: bearer
  header: Authorization
  token_format: JWT
  jwks: https://auth.internal/.well-known/jwks.json
requirements:
  - 所有请求必须带 X-Request-Id
  - 时钟偏差 < 30s
errors:
  401: invalid_token
  403: forbidden
events:
  - user.created
  - token.revoked
contact: auth-team@example.com
要求：

支持版本化，历史版本可查。

发布时校验 Schema。

支持兼容性检查。

支持变更摘要生成。

FR-03 Contract 管理
契约描述接口、协议、认证方式、通信要求、错误码、事件。

支持 JSON / YAML。

支持版本对比。

支持 harbor.check_compat 检查新旧版本兼容性。

FR-04 发现与查询
支持按 Berth、能力、标签搜索。

支持获取指定 Berth 的 Manifest 和 Contract。

支持解析依赖：某 Agent 需要哪些 Berth。

支持返回指定版本或最新版本。

FR-05 订阅与通知
消费 Agent 可订阅 Berth 或事件。

契约变更时，Harbor 通知订阅者。

通知只带摘要，不带完整契约。

支持通知优先级、合并、审计。

支持 MCP 原生通知和轮询兜底。

FR-06 会话内通知处理
Agent 运行时维护事件队列：

text
MCP Client 收到通知
  -> 写入 event_queue
  -> 会话循环 before_next_step 检查
  -> 注入 system/internal_event
  -> Agent 决策：
       - 标记缓存失效
       - 调 harbor.get_manifest()
       - 切换版本
       - 告知用户
       - 中断高优先级任务
通知注入示例：

json
{
  "role": "system",
  "content": "Harbor 通知：auth 契约从 1.2.0 更新到 1.3.0。摘要：新增 token.revoked 事件。建议下次调用 auth 前刷新契约。"
}
FR-07 缓存与同步
Agent 本地缓存契约，带 TTL。

收到通知后标记缓存失效。

下次调用前按需拉取。

支持 harbor.sync 手动同步。

支持 harbor.check_updates 轮询兜底。

FR-08 审计
所有发布、查询、订阅、通知操作写审计日志。

日志保留至少 90 天。

支持按 Berth、操作者、时间查询。

8. MCP 接口
8.1 Resources，只读
text
harbor://berths
harbor://berths/{berth}/manifest
harbor://berths/{berth}/contracts/{version}
8.2 Tools，写和交互
text
harbor.publish_manifest
harbor.get_manifest
harbor.search_berths
harbor.subscribe
harbor.notify
harbor.resolve_dependency
harbor.check_compat
harbor.check_updates
harbor.sync
8.3 Notifications
客户端订阅：

text
resources/subscribe
  uri: harbor://berths/auth/manifest
服务端通知：

text
notifications/resources/updated
  uri: harbor://berths/auth/manifest
  params: {
    berth,
    old_version,
    new_version,
    change_type,
    severity,
    summary
  }
8.4 Prompts，可选
text
onboard_berth
check_compat
9. 关键流程
9.1 接入流程
项目 Agent 调 harbor.publish_manifest。

Harbor 校验并存储。

其他 Agent 调 harbor.get_manifest 或 harbor.search_berths。

按契约直接通信。

9.2 调用流程
Agent 启动时拉取依赖契约。

执行任务时优先使用本地缓存。

缓存过期或收到通知时重新拉取。

业务调用直连目标模块，不经过 Harbor。

9.3 变更通知流程
text
认证 Agent 发布 v1.3.0
  -> harbor.publish_manifest("auth", v1.3.0)
  -> Harbor 匹配订阅
  -> 发 notifications/resources/updated
  -> 订单 Agent MCP Client 收到
  -> 写入事件队列
  -> 会话安全边界注入内部事件
  -> 订单 Agent 标记 auth 缓存失效
  -> 下次调用前 harbor.get_manifest("auth")
  -> 拿到 v1.3.0，更新缓存
  -> 继续直连 auth
9.4 降级流程
Harbor 挂了：Agent 用本地缓存继续直连。

通知丢了：TTL 过期或 check_updates 轮询补。

契约不兼容：暂停任务，重新规划或问用户。

通知风暴：5 秒内同一 Berth 合并通知。

10. 通知机制详细设计
10.1 通知内容
通知只带摘要：

json
{
  "berth": "auth",
  "old_version": "1.2.0",
  "new_version": "1.3.0",
  "change_type": "contract_changed",
  "severity": "normal",
  "summary": "新增 token.revoked 事件，错误码 429",
  "resource_uri": "harbor://berths/auth/manifest"
}
完整契约由 Agent 按需拉取。

10.2 优先级
优先级	场景	处理方式
low	普通字段变更	下一轮处理
normal	接口/错误码变更	下次调用前刷新
high	认证方式、密钥、安全漏洞	立即中断并处理
10.3 合并策略
同一 Berth 短时间内多次变更，合并为一条，只推最新版。

10.4 会话一致性
会话记录 contract_pins：当前任务固定使用哪个版本。

不兼容：暂停，重新规划或问用户。

兼容：完成当前事务后切换。

安全紧急：立即切换。

避免同一任务一半用旧契约、一半用新契约。

11. 数据模型
text
berths
manifests
contracts
subscriptions
notifications
audit_logs
订阅表示例：

text
subscription_id
subscriber_id
berth
version_range
events
callback_uri / resource_uri
created_at
12. 非功能需求
类别	要求
安全	MCP 连接需认证，支持 Token / mTLS
版本化	Manifest 和 Contract 必须可追溯
可用性	Harbor 不可用时，已获取契约的 Agent 仍可直连通信
性能	查询契约 P95 < 200ms
存储	MVP 可用 SQLite + JSON，后续可换 PostgreSQL
审计	操作日志保留至少 90 天
扩展性	支持新增 Berth、事件类型、通知通道
降级	本地缓存、轮询、手动同步
13. MVP 范围
必须实现：

Manifest / Contract Schema。

MCP Harbor Server 骨架。

核心工具：publish、get、search、subscribe、check_updates。

认证模块接入并发布 Manifest。

其他 Agent 可查询认证契约。

基础通知与事件队列。

基础审计日志。

后续再做：

权限系统

Web UI

兼容检查

依赖图

多通知通道

高级合并策略

14. 里程碑
阶段	内容
M0	Schema + MCP Server 骨架
M1	Berth 注册、Manifest 发布、查询
M2	订阅、通知、事件队列、审计
M3	权限、兼容检查、Web UI、依赖图
15. 验收标准
认证模块可注册 berth=auth，发布 Manifest v1.2.0。

订单 Agent 可通过 MCP 查询 auth，获取 base_url、auth.type、header、jwks、错误码。

auth 发布 v1.3.0 时，订阅者收到通知。

订单 Agent 在会话中收到通知，标记缓存失效，并在下次调用前拉取 v1.3.0。

所有发布、查询、订阅操作有审计日志。

Harbor 不可用时，已获取契约的 Agent 仍可直连通信。

16. 风险与对策
风险	对策
契约漂移	版本化 + 兼容检查
权限混乱	owner 制 + 审计
Harbor 单点	本地缓存 + 只读降级
通知泛滥	优先级 + 合并 + 订阅粒度
会话上下文爆炸	通知只带摘要，按需拉取
任务中途契约变更	contract_pins + 兼容检查
数据膨胀	只存元数据，不存业务数据
17. 一句话总结
MCP Harbor 是各 Agent 项目的“契约港”。
每个项目一个泊位，发布 Manifest 和 Contract；其他 Agent 通过 MCP 查询、订阅、接收通知。
业务调用始终直连，不经过 Harbor。
变更时，Harbor 发摘要通知，Agent 写入事件队列，在会话安全边界注入内部事件，再按需拉取最新契约。
订阅通知负责“知道变了”，按需拉取负责“拿到最新”，本地缓存负责“平时不打扰 Harbor”。